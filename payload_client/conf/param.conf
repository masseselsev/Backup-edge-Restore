# Trigger copy-payload script during init-bottom phase
if [ "$initdir" = "/scripts/init-bottom" ]; then
    if [ ! -f /tmp/payload_copied ]; then
        touch /tmp/payload_copied
        if [ -x /scripts/init-bottom/copy-payload ]; then
            /scripts/init-bottom/copy-payload
        fi
    fi
fi
